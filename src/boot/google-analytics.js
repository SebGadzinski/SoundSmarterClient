import { boot } from "quasar/wrappers";
import VueGtag from "vue-gtag";

// TODO: If you want to use gtag
export default boot(({ app }) => {
	// app.use(VueGtag, {
	// 	config: { id: "G-XXXXXXXX" },
	// });
});
